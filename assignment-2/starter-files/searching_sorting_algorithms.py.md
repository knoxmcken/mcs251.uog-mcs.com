# searching\_sorting\_algorithms.py

```python
def linear_search(arr, target):
    # Implement the Linear Search algorithm to find the index of the target in arr.
    # Return the index if found, otherwise return -1.


def binary_search(arr, target):
    # Implement the Binary Search algorithm to find the index of the target in the sorted arr.
    # Return the index if found, otherwise return -1.


def bubble_sort(arr):
    # Implement the Bubble Sort algorithm to sort arr in ascending order.
    # Return the sorted arr.


def quick_sort(arr):
    # Implement the Quick Sort algorithm to sort arr in ascending order.
    # Return the sorted arr.


def merge_sort(arr):
    # Implement the Merge Sort algorithm to sort arr in ascending order.
    # Return the sorted arr.

```
